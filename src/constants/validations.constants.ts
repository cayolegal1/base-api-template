import { globalMessage } from "src/i18n";

/* This messages targets to the i18n translation values, if you don't want to use it just change the values */

export const requiredMessage = globalMessage.required_message;
export const invalidEmailMessage = globalMessage.invalid_email_message;
export const invalidIdMessage = globalMessage.invalid_id_message;
export const invalidNameMessage = globalMessage.invalid_name_message;
